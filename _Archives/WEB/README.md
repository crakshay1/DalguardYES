# The react frontend for the BEPS - EvryRNA project revamped

## Running the code

This standard react stuff
`bash npm install` : to install all deps
`bash npm run dev` : to run the dev server
`bash npm run build` : to build the project
